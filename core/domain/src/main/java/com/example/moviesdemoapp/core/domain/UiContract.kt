package com.example.moviesdemoapp.core.domain

/** Marker interface for all MVI UI state classes. */
interface UiState

/** Marker interface for all MVI UI intent classes. */
interface UiIntent

/** Marker interface for all MVI UI effect classes (one-shot events). */
interface UiEffect
